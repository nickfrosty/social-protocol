/// max allowed length for common display names
pub const MAX_LEN_NAME: usize = 128;

/// max allowed length for usernames
pub const MAX_LEN_USERNAME: usize = 28;

/// max allowed length for off-chain uris (e.g. `metadata_uri` and `image_uri`)
pub const MAX_LEN_URI: usize = 256;
